/* function countActiveAstronauts
   Description: A function declared to a constant variable that returns the number of astronauts in a roster array
   Parameters:
     roster (Array) - An array/list of astronauts
   Return:
     The number of astronauts in the roster list
*/
const countActiveAstronauts = (roster) => {
  // Return roster list length
  return roster.length;
}
